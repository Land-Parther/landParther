import styled from 'styled-components'

const Container = styled.div`
  max-width: 850px;
  width: 100%;

  main {
    border: 1px solid #e1e4e8;
    border-radius: 6px;
    overflow: hidden;
  }

  footer {
    margin-top: 50px;
  }
`

export default {
  Container
}